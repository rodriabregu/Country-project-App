import { Pagination } from "../Pagination/";
import Search from '../Search';

function Home() {
    return (
        <div> 
            <Search />
            <Pagination />
        </div>
    );
};

export default Home;